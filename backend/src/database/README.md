# Database Module

Current responsibilities:

- Prisma schema
- local SQLite database
- seed data for demo conversations
- shared Prisma client

Current commands:

- `npm run db:generate`
- `npm run db:push`
- `npm run db:seed`
